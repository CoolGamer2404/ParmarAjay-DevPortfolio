using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoopTraps : MonoBehaviour
{
    public enum TrapType
    {
        LookBack,
        LookForward,
        LookUp,
        LookLeft,
        LookRight,
        LOL,
        OpenMe,
        DontOpen,
        YouAreDead,
        Die,
        DontLookAtMe,
        GoBack,
        IWillKillYou,
        YouAreTrapped,
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
